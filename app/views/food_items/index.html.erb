<br><h1>spis pokarmow</h1>
<table>
  <tr>
    <th>Nazwa</th>
    <th>kalorie</th>
    <th></th>
  </tr>

  <% @food_items.each do |food_item| %>
    <tr>
      <td><%= food_item.name %></td>
      <td><%= food_item.kcal %></td>
      <td><%= food_item.id %></td>
      <td><%= link_to 'Show', food_item_path(food_item) %></td>
    </tr>
  <% end %>
</table>

<p><i><%= link_to "Dodaj nowy pokarm do bazy", new_food_item_path  %></i></p>
